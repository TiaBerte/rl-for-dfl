# Reinforcement Learning for Decision-focused Learning

The code was tested on Python 3.7 and with packages version specified in the `requirements.txt` file.

You can either run the `main.py` script in the main working directory or the notebook file in the `notebooks` directory.